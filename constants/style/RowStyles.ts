import { StyleSheet } from "react-native";

export default StyleSheet.create({
    // se precisar de estilos para uma Row global, adicione aqui
    row: {
        flexDirection: "row",
        alignItems: "center",
    },
});
